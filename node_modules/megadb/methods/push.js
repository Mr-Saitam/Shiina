const { saveJSON } = require("../utils/utilidades.js"),
				errorDB = require("../class/errorDB.js");

module.exports = {
	push: function(clave, valor, split_object, global_object, self, save) {

	  if(!valor && valor != 0) throw new errorDB(`El valor no fue especificado`, 'VALOR INVALIDO', 'push', clave, valor)

	  var [...args] = clave.split(split_object)
	  var object_data = global_object[self.object_id]

		for(var key of args) {
			if(!object_data.hasOwnProperty(key)) return Promise.reject(new errorDB(`No se encontro la propiedad ${key}, base de datos: ${self.database_name}`, 'PROPIEDAD NO EXISTENTE', 'push', clave, valor))
			if(key == args[args.length-1]) {
				if(!Array.isArray(object_data[key])) return Promise.reject(new errorDB(`El valor de la propiedad ${key} no es un array, base de datos: ${self.database_name}`, 'ARRAY NO EXISTENTE', 'push', clave, valor))
				object_data[key].unshift(valor)
				if(!save) return Promise.resolve(object_data[key])
				saveJSON(self, global_object[self.object_id])
				return Promise.resolve(object_data[key])
			}
			else {
				object_data = object_data[key]
			}
		}
	}
}