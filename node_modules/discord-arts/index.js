const discordJS = require('../../package.json').dependencies['discord.js'] || require('../../package.json').devDependencies['discord.js'];

if(discordJS.startsWith("^13.")){
  module.exports = {
    profileImage : require('./src/Code/v13/profileImage')
  }
}else if(discordJS.startsWith("^14.")){
  module.exports = {
    profileImage : require('./src/Code/v14/profileImage')
  }
}

